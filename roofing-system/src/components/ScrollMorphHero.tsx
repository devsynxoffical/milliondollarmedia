import IntroAnimation from "./ui/scroll-morph-hero";

export function ScrollMorphHero() {
  return (
    <section className="relative w-full border-y border-zinc-100 bg-white">
      <IntroAnimation />
    </section>
  );
}
